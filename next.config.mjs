/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
        "assets.adidas.com",
        "imgnike-a.akamaihd.net",
        ],
      },
};

export default nextConfig;
